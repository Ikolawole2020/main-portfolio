# TODO — Neon Glass Portfolio Redesign

- [x] Fully replace layout + section conventions in `index.html` (new hero, glass cards, projects, and TicTacToe section).
- [x] Rewrite `style.css` with a new design system (neon glass, new components, new layout rules).
- [x] Rewrite `main.js` to match the new DOM and add a working TicTacToe game (PVP + vs CPU, restart, scores).
- [ ] Verify in browser:
  - [ ] Navigation highlights the active section.
  - [ ] Scroll progress bar updates.
  - [ ] Typed.js initializes.
  - [ ] TicTacToe works in both modes.
  - [ ] Restart works without breaking scores.
  - [ ] Contact form submits gracefully (currently no backend; should show no console errors).
- [ ] Optional polish:
  - [ ] Replace placeholder “Email” link behavior with a real mailto.



